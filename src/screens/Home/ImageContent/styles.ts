import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  image: {
    width: 220,
    height: 200,
    alignSelf: 'center',
    marginTop: 24,
  },
  contentImage: {
    alignItems: 'center',
  },
});
