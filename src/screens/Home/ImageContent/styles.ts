import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  image: {
    width: 180,
    height: 170,
    alignSelf: 'center',
    marginTop: 8,
  },
  contentImage: {
    alignItems: 'center',
  },
});
